const TestComponent = () => {
  return (
    <div
      style={{
        background: "red",
        width: "800px",
        height: "2000px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      TestComponent
    </div>
  );
};

export default TestComponent;
